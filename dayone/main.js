// Print a message indicating the usage of variables
console.log("Usage of variables"); //question two & three
// Define a variable and initialize it with a string value
var variable = "Sanika";
// Print a greeting message using the variable
console.log("Hello ".concat(variable, ", would you like to learn some TypeScript today?"));
// Print the variable in all lowercase letters
console.log(variable.toLowerCase()); // Shows the name in all small letters
// Print the variable in all uppercase letters
console.log(variable.toUpperCase()); // Shows the name in ALL BIG LETTERS
// Print the variable with the first letter capitalized and the rest in lowercase
console.log(variable.charAt(0).toUpperCase() + variable.slice(1).toLowerCase()); // Shows the name with the First Letter Big
