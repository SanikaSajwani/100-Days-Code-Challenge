// Question four, five & six

// Print a quote by Albert Einstein
console.log('Albert Einstein once said, “A person who never made a mistake never tried anything new.”');

// Define a variable for a famous person and a message using template literals
let famous_person: string = "Marie Curie";
let message: string = `${famous_person} once said, “One never notices what has been done; one can only see what remains to be done.”`;

// Print the message
console.log(message);

// Define a variable for a name with whitespace
let variable: string = "\t\n Sanika \t\n"; 

// Print the name with whitespace
console.log(variable); // Shows the name with whitespace

// Print the name without leading and trailing whitespace using the trim() method
console.log(variable.trim()); // Shows the name without whitespace
